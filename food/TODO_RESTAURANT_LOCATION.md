# TODO: Restaurant Location (Seller Profile)

- [x] `seller.html` me Settings page par “Get Current Location” button + hidden lat/lng inputs add kiye.
- [x] `seller.js` me `getRestaurantCurrentLocation()` implement kiya (geolocation capture -> rest-lat/rest-lng set).
- [x] `seller.js` me `loadSettings()` me saved `locationLat/locationLng` values populate kiye.
- [x] `seller.js` me `saveSettings()` me `locationLat/locationLng` store kiye; agar empty ho to previous values retain.

